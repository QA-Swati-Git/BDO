name: UI Web Tests Manual Run
on:
  workflow_dispatch:
    inputs:
      env:
        description: 'Choose Repo Branch:'
        required: true
        default: 'main'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-java@v3
      with:
       distribution: 'temurin'
       java-version: '17'
    - name: Build & Install
      run: mvn --file BDO_Test/pom.xml -B install -D skipTests --no-transfer-progress
    - name: Install Playwright
      run: mvn --file BDO_Test/pom.xml exec:java -e -D exec.mainClass=com.microsoft.playwright.CLI -D exec.args="install --with-deps"
    - name: Run tests
      run: mvn --file BDO_Test/pom.xml test
